parserOptions:
  ecmaVersion: latest
  sourceType: "module"

env:
  es2022: true
  browser: true

overrides:
  - files:
      - "src/config.js"
    globals:
      process: "readonly"

extends: "htmlacademy/vanilla"
